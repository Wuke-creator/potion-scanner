services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    ports:
      - "${OAUTH_PORT:-8080}:8080"
    stop_grace_period: 10s
    restart: unless-stopped
